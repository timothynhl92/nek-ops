# Naming convention

The authoritative rules are **README §5** (on-document reference, filename
format, and the sequence-counter contract), cross-checked against sheet
`07 Naming Convention` in the master registers.

As with house style, this file records post-handoff decisions and edge cases
rather than restating §5.

Nothing to record yet.
